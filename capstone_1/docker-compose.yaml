services:
  room-classifier-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: room-classifier-api
    ports:
      - "9696:9696"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9696/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    labels:
      - "com.example.description=Room Classifier API"
      - "com.example.version=1.0.0"